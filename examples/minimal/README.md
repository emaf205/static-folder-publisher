# Minimal example

This directory is a complete zero-configuration content example.

From the repository root, copy `examples/minimal/content/` into an otherwise empty project directory, then run the installed publisher against that project:

```bash
publisher --project /path/to/project build
```

No `config.json` is required for a local build.
